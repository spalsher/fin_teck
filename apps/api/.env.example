# Database (PostgreSQL)
# Full format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME?schema=public
# If password contains $ or #, URL-encode: $ → %24, # → %23
# Example remote: postgresql://employee_dev:PASSWORD_ENCODED@192.168.21.31:6632/iteck_erp?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iteck_erp?schema=public"

# SQL Server (source) for one-time migration: scripts/migrate-sqlserver-to-pg
# Example: Server=192.168.20.199;Database=ATS_GL;User Id=alfatech;Password=YOUR_PASSWORD;TrustServerCertificate=true;
# MIGRATION_SQL_SERVER_URL=

# API
API_PORT=3001
API_PREFIX=api
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=3600

# CORS (comma-separated origins, or leave empty to allow localhost in dev)
# CORS_ORIGIN=http://localhost:3000,http://localhost:3002
