# Leave unset to use same-origin /api (Next.js proxies to backend). Set to full URL to call API directly.
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Backend URL for Next.js rewrites (server-side). Default: http://127.0.0.1:3001
# API_PROXY_TARGET=http://127.0.0.1:3001
